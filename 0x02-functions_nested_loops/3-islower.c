#include "main.h"
/**
 * main - Entry point
 * Description: 'prints _putchar to stdout'
 * Return: always 0
 */
int -islower(int c)
{
	if (c <= 97; && c <= 122)
	{
		return (1);
	}
	else
	{
		return(0);
	}
	putchar('\n');
}
